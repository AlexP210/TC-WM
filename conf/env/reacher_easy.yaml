name: reacher_easy
args: []
kwargs: {}

dataset:
  _target_: "datasets.mmbench_dset.load_mmbench_slice_train_val"
  n_rollout: null
  normalize_action: ${normalize_action}
  data_path: /mnt/data_7tb/minghao.fu/newt_rollouts/reacher-easy-nototarget
  split_ratio: 0.8
  image_size: ${img_size}
  transform:
    _target_: "datasets.img_transforms.default_transform"
    img_size: ${img_size}

decoder_path: null
num_workers: 16
