_target_: models.decoder.vqvae.DirectVQVAEDecoder
channel: 64
n_embed: 2048
n_res_block: 4
n_res_channel: 128
quantize: false
sg_input: true        # stop-grad before VQVAE (frozen encoder path)
# emb_dim injected at runtime from encoder_emb_dim (384 for DINO)
